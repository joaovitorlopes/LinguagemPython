nota1 = float(input("Insira a primeira nota: "))
nota2 = float(input("Insira a segudna nota:"))
media = (nota1+nota2)/2
if media >= 6:
    print(f"O aluno está aprovado! com {round(media,2)} de média")
else:
    print(f"O aluno está reprovado! com {round(media,2)} de média")
